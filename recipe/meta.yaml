{% set name = "wagtail-nav-menus" %}
{% set version = "3.11.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/wagtail_nav_menus-{{ version }}.tar.gz
  sha256: e68dfef523d9ac7a42a6e3d2910e722ffd659582ba93f755f94bc29a53e450ba

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.10,<4.0
    - poetry-core >=1.9.0
    - pip
  run:
    - python >=3.10.0,<4.0.0
    - wagtail >=5.2,<6.0

test:
  imports:
    - wagtail_nav_menus
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://gitlab.com/thelabnyc/wagtail-nav-menus
  summary: Wagtail Nav Menus is a app to provide highly customizable menus in wagtail by leveraging StreamFields.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - rxm7706
